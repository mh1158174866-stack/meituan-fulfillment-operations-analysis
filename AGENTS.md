# 项目级执行规则

## 项目目标

在 3—5 个专注工作日内完成“美团外卖履约经营分析：骑手效率、供需峰值与运力配置诊断”的完整首版。项目主线是经营分析、SQL、指标体系和可复现交付，不是路线优化论文或单纯预测项目。

## 当前阶段

第一、二阶段已验收。当前已完成：

1. 官方数据、质量、表关系、业务时间与参考 KPI 复核；
2. 7 个轻量 DuckDB 核心对象：清洗运单、订单—波次桥、修正波次与四张聚合分析表；
3. 接单率、配送/全链路时长、距离、超时、波次时长、空闲、负载与供需容量代理口径；
4. 一键双重重建、结构/指标校验、语义指纹与隐私扫描。

未经新的明确开始指令，不进入阶段三业务结论、正式看板、预测模型、Agent 或策略包装。A/B Test 只能作为后续线上验证设计，不得将离线代理写成真实实验结果。

## 数据与许可红线

- 数据于 2024 年公开，底层业务订单主要发生在 2022 年 10 月；两者必须分开表述。
- 遵守官方 CC BY-NC 4.0、仅限非商业用途、署名和不得重新分发要求。
- 不向 Git 提交官方 CSV、压缩包、PDF 数据附件、原始 ID、坐标明细或可还原行为序列。
- 公开产物只允许字段说明、规模、聚合指标、脱敏图表和官方下载指引。
- 首次生成 CSV、JSON 或 Parquet 后必须运行隐私扫描并人工复核。
- 公开研究致谢使用：“本研究由美团提供数据支持。”

## 真实性与口径规则

- 参考报告的 84%、27 分钟等数值只作为验收基准，不作为本项目成果。
- README、报告和看板只能使用本项目实际运行结果。
- 不为匹配参考结果而无依据调整过滤条件；所有差异必须记录口径解释。
- 明确订单与运单粒度、时间节点、时区、异常过滤、拒单/重复派单/预订单/跨区规则。
- 同一 KPI 至少保留计算公式、分母、过滤条件、代码入口和结果日期。

## 工程与交付规则

- 原始数据只读；清洗结果写入 `data/interim/` 或 `data/processed/`。
- ODS/DWD/DWS/ADS SQL 分层分别放在 `sql/` 对应目录。
- 可复现脚本放在 `scripts/`，分析逻辑放在 `src/`。
- 每个阶段在 `docs/阶段记录.md` 记录命令、结果、口径问题、验收结论和待办。
- 交付前检查运行结果、隐私扫描、Git 状态以及 README、报告、看板和指标一致性。
- 未经用户明确授权，不提交或推送 Git；创建远程仓库后也先检查待提交文件。
