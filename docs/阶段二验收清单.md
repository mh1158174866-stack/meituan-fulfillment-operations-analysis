# 第二阶段验收清单

状态标记：`[ ]` 未开始，`[x]` 已验收，`[!]` 保留边界。

## A. 环境与输入

- [x] Git 为 `main`，阶段开始时尚无任何提交，已保留用户现有未跟踪文件。
- [x] 官方四表与说明附件均在 `data/raw/` 且只读使用。
- [x] `.venv` 使用 Python 3.12.13，新增并固定 `duckdb==1.4.1`。
- [x] 阶段二只依赖全运单和波次表；派单候选表不被误解为分配事实。

## B. 数据对象

- [x] `dwd_waybill` 为 654,343 条运单，`waybill_id` 无重复。
- [x] `dwd_order_wave_bridge` 为 568,545 条波次订单引用，复合键无重复，孤儿关联为 0。
- [x] `dwd_courier_wave` 为 206,748 个波次，复合主键无重复。
- [x] 经营总览 13 行、分时供需 223 行、骑手效率 4,955 行、异常诊断 11 行。
- [x] ODS 为临时只读映射，不建设冗余企业数仓。

## C. 状态、时间与波次

- [x] 85,797 条拒绝、568,545 条完成、1 条已接受未完成运单分开处理。
- [x] 保留 `dt`、Unix 秒、UTC 时间、Asia/Shanghai 时间、下单日和履约请求日。
- [x] 65,904 个波次起点差异使用最早 `grab_time` 修正，原值与差异标记保留。
- [x] 波次终点差异为 0；修正波次时长无负值。

## D. 指标契约

- [x] 接单率、订单完成率、配送时长、全链路时长、距离和超时率均保留公式、分母、过滤和有效样本量。
- [x] 波次时长、空闲时长、波次负载和小时供需容量代理均有可审计公式。
- [x] 运单加权接单率 86.8881%、骑手等权接单率 84.7436%、骑手等权配送时长 27.10 分钟均回勾通过。
- [!] 专区/多区域和跨区率未纳入，因 `recipient_area` 构造不可审计。
- [!] 小时容量仅为离线代理，不是官方产能、策略效果或 A/B Test 结果。

## E. 工程、确定性与隐私

- [x] `scripts/run_stage2.sh` 可一键构建、验证、重建、比较指纹并执行隐私扫描。
- [x] 两次全量重建的 7 张表结构、行数和全表语义哈希完全一致。
- [x] 指纹 JSON 只保存结构、行数和哈希，不包含原始值或 ID。
- [x] 40 个 Git 候选文件通过隐私扫描；CSV、ZIP、PDF 数据附件、DuckDB、ID、坐标和明细序列不可见。
- [x] README、数据字典、质量报告和阶段记录与实际运行数字一致。

## F. 范围验收

- [x] 未进入正式业务结论、看板、预测模型、Agent、策略包装或真实 A/B Test。
- [x] 第二阶段验收通过。
